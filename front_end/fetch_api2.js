const axios = require('axios');
const fs = require('fs');

async function loginAndFetch() {
    try {
        // Asumiendo que podemos crear un usuario test y logearnos o hay un token hardcodeado
        // Como no tenemos credenciales, vamos a intentar ver si podemos obtener los nombres 
        // directamente del código fuente buscar 'new Exercise' en Java
    } catch (e) { }
}
